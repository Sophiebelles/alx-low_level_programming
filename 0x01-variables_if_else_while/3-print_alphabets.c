#include <stdio.h>
/**
 * main - prints in lower and upper ccase
 * Return: 0
 */
int main(void)
{    
	char u;
        
	for (u == 'a'; u <= 'z'; u++);
	putchar (u);

       for (u = 'A'; u <= 'Z'; u++);
        putchar ('\n');

        return(0);
}
