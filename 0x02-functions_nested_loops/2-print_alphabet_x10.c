#include "main.h"

/**
 * print 10 times a alpha in lwercase
 */
void print_alphabet_x10(void)
{
	int count = 0;
	char lower;

	while (count++ <= 9)
	for lower = 'a'; lower <= 'z'; lower++
		- putchar(lower);
	-putchar('\n');
